import { Box } from "@mui/material";

export default function About() {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "column",
        maxWidth: 640,
        mx: "auto",
        mt: 4,
      }}
    ></Box>
  );
}
