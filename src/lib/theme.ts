import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#f5f5f3",
      main: "#1D9E75",
      dark: "#1a1a2e",
    },
  },
});

export default theme;
